# Web framework
fastapi==0.136.3
uvicorn[standard]==0.49.0

# Database
sqlalchemy[asyncio]==2.0.50
asyncpg==0.31.0
alembic==1.18.4

# Validation & settings
pydantic==2.13.4
pydantic-settings==2.14.1
email-validator==2.3.0

# Security
pyjwt==2.13.0
bcrypt==5.0.0
cryptography==49.0.0

# Redis
redis==8.0.0

# Background jobs (broker/backend both Redis; keep redis==8 pinned above)
celery==5.6.3

# Email (MailHog in docker-compose; no TLS needed locally)
aiosmtplib==5.1.2

# Object storage
minio==7.2.20

# PDF generation (pure Python: no native deps to install in the slim image)
reportlab==5.0.0

# Misc
python-multipart==0.0.32
